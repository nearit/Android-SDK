<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.near.sdk">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name">

        <!-- region messages -->

        <!-- GCM registration -->

        <!-- push messages -->
        <!-- service that receives push from GCM and does internal SDK work -->
        <service
            android:name="it.near.sdk.push.MyFcmListenerService"
            android:exported="false" >
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- internal service to send local notifications -->
        <service android:name=".recipes.background.NearItIntentService"
            android:exported="false"/>

        <service
            android:name=".geopolis.geofences.GeoFenceService"
            android:exported="false" />
        <service android:name=".geopolis.geofences.NearGeofenceTransitionsIntentService"
            android:exported="false" />
        <receiver android:name=".geopolis.geofences.GeoFenceSystemEventsReceiver">
            <intent-filter>
                <action android:name="android.location.PROVIDERS_CHANGED"/>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>


        <receiver android:name=".trackings.BluetoothStatusReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.bluetooth.adapter.action.STATE_CHANGED"/>
            </intent-filter>
        </receiver>

        <service
            android:name=".push.fcmregistration.MyInstanceIDListenerService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>


    </application>

</manifest>
